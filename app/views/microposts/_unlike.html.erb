<%#= link_to "unlike", micropost_favorites_path(micropost), method: :delete, remote: true %>

<%= form_for(current_user.favorites.find_by(micropost_id: micropost.id), html: { method: :delete }, remote: true) do |f| %>
  <%= hidden_field_tag :micropost_id, micropost.id %>
  <%= f.submit 'Unlike', class: 'btn btn-warning btn-xs'%>
<% end %>