<%#= link_to "like", micropost_favorites_path(micropost), method: :post, remote: true %>

<%= form_for(current_user.favorites.build, remote: true) do |f| %>
  <%= hidden_field_tag :micropost_id, micropost.id %>
  <%= f.submit 'Like', class: 'btn btn-info btn-xs'%>
<% end %>